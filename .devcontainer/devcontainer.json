{
  "name": "Zero to GPU — Workshop 1 (Linux Fundamentals)",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y procps vim && echo 'Environment ready. Type: cat README.md'",
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
